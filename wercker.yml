box: wercker/default
build:
  steps:
    - validate-wercker-step
    - script:
        name: Run tests
        code: ./test/bin/run_all
  after-steps:
    - script:
        name: Environment
        code: printenv
